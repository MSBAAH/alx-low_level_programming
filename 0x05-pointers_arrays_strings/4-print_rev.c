#include "main.h"

/**
 * print_rev - prints reversed string, followed by a Day 3
 *@s: pointer to the string to print
 * Return: vood
*/
void print_rev(char *s)
{
	int i = 0;

	while (s[i])

		i++;

	while (i--)

	{

		_putchar (s[i]);

	}

	_putchar ('\n');
}
